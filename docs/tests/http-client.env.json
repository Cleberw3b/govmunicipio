{
  "$shared": {
    "prodUrl": "https://api-production-eb2b7.up.railway.app/api/v1",
    "stagingUrl": "http://localhost:3001/api/v1"
  },
  "prod": {
    "baseUrl": "{{$shared prodUrl}}",
    "superadminUsername": "superadmin",
    "superadminPassword": "{{$processEnv SUPERADMIN_PASSWORD}}",
    "adminUsername": "{{$processEnv ADMIN_USERNAME}}",
    "adminPassword": "{{$processEnv ADMIN_PASSWORD}}"
  },
  "staging": {
    "baseUrl": "{{$shared stagingUrl}}",
    "superadminUsername": "superadmin",
    "superadminPassword": "{{$processEnv SUPERADMIN_PASSWORD}}",
    "adminUsername": "{{$processEnv ADMIN_USERNAME}}",
    "adminPassword": "{{$processEnv ADMIN_PASSWORD}}"
  }
}
